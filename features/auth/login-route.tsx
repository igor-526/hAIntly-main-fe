"use client";
import { useEffect } from "react"; import { useRouter } from "next/navigation"; import { useAuth } from "./auth-provider"; import { AuthForm } from "./auth-form";
export function LoginRoute() { const auth=useAuth(); const router=useRouter(); useEffect(()=>{if(auth.status==="authenticated") router.replace("/");},[auth.status,router]); if(auth.status==="checking"||auth.status==="authenticated") return <Status text="Проверяем сессию…"/>; if(auth.status==="error") return <Status text={auth.error || "Ошибка"} retry={auth.check}/>; return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-4"><AuthForm/></main>; }
export function Status({text,retry}:{text:string;retry?:()=>unknown}) { return <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-slate-100 p-4" role="status"><p>{text}</p>{retry&&<button onClick={()=>retry()} className="rounded-lg bg-indigo-700 px-4 py-2 text-white">Повторить</button>}</main>; }
