"use client";
import { useEffect } from "react"; import { useRouter } from "next/navigation"; import { useAuth } from "./auth-provider"; import { Status } from "./login-route";
export function ProtectedHome(){const auth=useAuth();const router=useRouter();useEffect(()=>{if(auth.status==="anonymous")router.replace("/login");},[auth.status,router]);if(auth.status==="checking"||auth.status==="anonymous")return <Status text="Проверяем сессию…"/>;if(auth.status==="error")return <Status text={auth.error||"Ошибка"} retry={auth.check}/>;return <main className="flex min-h-screen items-center justify-center bg-slate-100 p-4"><section className="rounded-2xl bg-white p-10 text-center shadow"><h1 className="text-3xl font-bold text-indigo-700">HAIntly</h1><p className="mt-3 text-slate-600">Главная страница скоро появится.</p></section></main>}
